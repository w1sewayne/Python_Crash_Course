# 🐍 Python Chapter 3: 列表 (List) 速查表

> **核心概念**：列表就像一个“大书架”，有序地存放各种东西。
> **符号**：使用方括号 `[]`，元素之间用逗号 `,` 分隔。

## 1. 基础操作 (CRUD)

假设我们有一个列表： `guests = ['Alex', 'Bruce', 'Charlie']`

| 功能 | 英文关键字 | 代码示例 | 备注 |
| :--- | :--- | :--- | :--- |
| **访问** | Index | `guests[0]` | 拿到 'Alex' |
| **访问末尾** | Index -1 | `guests[-1]` | 拿到最后一个 ('Charlie') |
| **修改** | Assignment | `guests[0] = 'Amy'` | 'Alex' 变成了 'Amy' |
| **添加 (末尾)** | `append` | `guests.append('David')` | **最常用！**加到最后 |
| **添加 (插队)** | `insert` | `guests.insert(0, 'Zack')` | 插到开头，其他人后移 |
| **删除 (按位置)** | `del` | `del guests[0]` | 删了就没了，不需要它了 |
| **删除 (弹出)** | `pop` | `who = guests.pop()` | 删了还需要用它 (存给变量) |
| **删除 (按值)** | `remove` | `guests.remove('Bruce')` | 只知道名字，不知道位置 |

---

## 2. 排序与组织 (最容易混淆!)

假设： `cars = ['bmw', 'audi', 'toyota']`

### 🅰️ 排序 (A-Z)
| 方法 | 语法 | 是否修改原列表? | 记忆口诀 |
| :--- | :--- | :--- | :--- |
| **永久排序** | `cars.sort()` | **是 (Yes)** | **整理书架**：直接动手改，改完回不去 |
| **临时排序** | `sorted(cars)` | **否 (No)** | **拍照整理**：原书架不动，生成新清单 |
| **倒序开关** | `reverse=True` | - | `cars.sort(reverse=True)` (Z-A) |

### 🅱️ 反转 (单纯掉头)
* **语法**：`cars.reverse()`
* **注意**：这不是排序！只是把车头变车尾，物理翻转。

### 📏 长度
* **语法**：`len(cars)`
* **结果**：`3` (从 1 开始数，不是从 0)

---

## 3. 💣 常见地雷区 (Error Handling)

### ❌ IndexError: list index out of range
* **原因**：你想拿第 4 个人，但列表里只有 3 个人。
* **场景**：
    ```python
    guests = ['A', 'B']
    # print(guests[2])  <-- 报错！因为最大索引是 1
    ```
* **特别注意**：当你使用 `del` 或 `pop` 后，列表变短了，原来的索引可能就失效了！